{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:api-server",
    "build:api-server": "cd artifacts/api-server && npm run build"
  },
  "private": true,
  "devDependencies": {
    "prettier": "3.8.3",
    "typescript": "5.9.3"
  }
}
